<!-- governance: v1.0.0 | thin adapter — do not expand -->

Read `AGENTS.md` in the repository root first — it is the canonical constitution
for all AI agents in `ehudso7/momentum-trading-bot` and overrides these instructions on any conflict.

- Build/test/lint using only the verified commands in `AGENTS.md` §2.
- Never commit to `main` directly; never commit secrets or `.env*` files.
- Do not weaken tests, linters, or CI to get a passing result.
