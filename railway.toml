[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "[deploy]
startCommand = "/bin/sh -c \"python -m trading_bot.api.keys issue --tier free --label railway-smoke-test --manifest-path /data/api_keys_manifest.jsonl && sleep 300\""
healthcheckPath = "/health"
healthcheckTimeout = 5
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3"
healthcheckPath = "/health"
healthcheckTimeout = 5
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
